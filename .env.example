# ─────────────────────────────────────────────────────────────
# Plexo — Environment Configuration
# Copy to .env and fill in the values
# ─────────────────────────────────────────────────────────────

# ── Required ─────────────────────────────────────────────────

# Database password for PostgreSQL
# Generate: openssl rand -hex 32
POSTGRES_PASSWORD=

# Public URL including protocol (used for OAuth callbacks, links)
PUBLIC_URL=https://plexo.yourdomain.com

# Domain without protocol (used for Caddy, CORS)
PUBLIC_DOMAIN=plexo.yourdomain.com

# Session encryption secret
# Generate: openssl rand -hex 64
SESSION_SECRET=

# Primary AI provider — console.anthropic.com
ANTHROPIC_API_KEY=

# ── Optional AI Providers (fallback chain) ───────────────────

# OpenAI fallback — platform.openai.com
OPENAI_API_KEY=

# Gemini fallback — aistudio.google.com
GEMINI_API_KEY=

# ── GitHub OAuth (enables GitHub login + GitHub connection) ──

# Create at: github.com/settings/applications/new
# Callback URL: ${PUBLIC_URL}/api/oauth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ── Slack OAuth (enables Slack connection) ───────────────────

# Create at: api.slack.com/apps → OAuth & Permissions
# Redirect URL: ${PUBLIC_URL}/api/oauth/slack/callback
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=

# ── Google OAuth (enables Google Drive connection) ───────────

# Create at: console.cloud.google.com → Credentials → OAuth 2.0 Client
# Redirect URL: ${PUBLIC_URL}/api/oauth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── Sprint Engine ────────────────────────────────────────────

# Max concurrent sprint worker containers (reduce if VPS is constrained)
MAX_SPRINT_WORKERS=5

# ── Cost Controls ────────────────────────────────────────────

# Tasks auto-pause when weekly AI spend reaches this amount
API_COST_CEILING_USD=10.00

# ── Database (auto-configured by Docker Compose) ────────────

# Only override if running Postgres outside Docker
# DATABASE_URL=postgresql://plexo:${POSTGRES_PASSWORD}@localhost:5432/plexo

# ── Redis (auto-configured by Docker Compose) ───────────────

# Only override if running Redis outside Docker
# REDIS_URL=redis://localhost:6379
